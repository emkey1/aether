mod Calc {
    export fn timesTwo(x: Int) -> Int {
        ret x * 2;
    }

    export fn timesFour(x: Int) -> Int {
        ret timesTwo(timesTwo(x));
    }

    export fn timesFourSelfQualified(x: Int) -> Int {
        ret Calc.timesTwo(Calc.timesTwo(x));
    }
}
