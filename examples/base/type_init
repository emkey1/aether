type Point {
    x: Int;
    y: Int;

    fn sum() -> Int {
        ret self.x + self.y;
    }
}

fn main() -> Void {
    let point: Point = Point { x: 12, y: 30 };

    fx {
        println("point sum = ", point.sum());
    }
    ret;
}
