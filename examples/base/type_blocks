type Point {
    x: Int;
    y: Int;

    fn sum() -> Int {
        ret self.x + self.y;
    }
}

fn main() -> Void {
    let point: Point = new Point();
    point.x = 19;
    point.y = 23;

    fx {
        println("point sum = ", point.sum());
    }
    ret;
}
