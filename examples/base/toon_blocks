fn main() -> Void {
    let payload: TOON = toon:
      users[2]{id,name,role}:
        1,Ada,admin
        2,Bob,user

    fx {
        println("official TOON payload:");
        println(payload);
    }
    ret;
}
