@pre value >= 0
@post result > value
fn next(value: Int) -> Int {
    ret value + 1;
}

fn main() -> Void {
    let start: Int = 7;
    let finish: Int = next(start);
    fx {
        println("next(", start, ") = ", finish);
    }
    ret;
}
